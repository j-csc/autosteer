```
# setup virtual environment
python -m venv venv
source venv/bin/activate

# install dependencies
pip install -r requirements.txt
```

Plan:
Create SAE steering sample on colab
Integrate auto steering functionality, potentially without HookedTransformers but only standard transformer library
Modal labs deployment for demo